<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BudgetAppTry2.Views.MainPage"
             xmlns:model="clr-namespace:BudgetAppTry2.Models"
             Title="Budget Amount">
    
        
   
    <ContentPage.ToolbarItems>


        <ToolbarItem   Text="EnterYourBudget"
                     Order="Primary"
                     Priority="0"
                     Clicked="ToolbarItem_Clicked"/>

       <ToolbarItem Text="bgt" IconImageSource="Resources/drawable/BudgetIcon.jpg"
                   Order="Secondary"   
                    />

        
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        
        <StackLayout>
            
            
            <ListView  x:Name="Amtset" 
              ItemSelected="Amtset_ItemSelected"
                      HeightRequest="50">

                <ListView.ItemTemplate>
            <DataTemplate x:DataType="model:ExpenseCategoryIcon">
                        
                        <TextCell Text="{Binding Text}"
                          Detail="{Binding Date}"/>
                        

                    </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
        </StackLayout>
        
    </ContentPage.Content>
</ContentPage>